import { Route } from '@angular/router';
import { ComponentDirectiveComponent } from './component-directive.component';

export const directiveRoute: Route = {
  path: 'component-directive',
  component: ComponentDirectiveComponent
};